(()=>{
function esc(s){return String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]))}
function stripFrontMatter(text){return String(text||'').replace(/^---\s*[\s\S]*?\n---\s*/,'')}
function firstSlide(text){const body=stripFrontMatter(text);const parts=body.split(/^---\s*$/m);const slide=(parts.find(p=>/^#{1,2}\s+/m.test(p))||body).trim();const titleMatch=slide.match(/^#{1,2}\s+(.+?)(?:\s+\{.*\})?$/m);const title=titleMatch?titleMatch[1].replace(/[\*_`]/g,'').trim():'PASQAL Quarkfoil deck';const afterTitle=titleMatch?slide.slice((titleMatch.index||0)+titleMatch[0].length):slide;const subtitleMatch=afterTitle.match(/^##\s+(.+?)(?:\s+\{.*\})?$/m);const subtitle=subtitleMatch?subtitleMatch[1].replace(/[\*_`]/g,'').trim():'';const bullets=[...afterTitle.matchAll(/^\s*[-*]\s+(.+)$/gm)].slice(0,4).map(m=>m[1].replace(/[\*_`]/g,''));const bold=[...afterTitle.matchAll(/\*\*(.+?)\*\*/g)].map(m=>m[1]).filter(x=>x!==title).slice(0,2);return {title,subtitle,bullets,bold}}
function updatePreview(text,name){const p=document.querySelector('.qf-preview');if(!p)return;const c=p.querySelector('.qf-content');if(!c)return;const s=firstSlide(text);let extra='';if(s.subtitle)extra+=`<p class="qf-preview-subtitle">${esc(s.subtitle)}</p>`;if(s.bold.length)extra+=`<p><strong>${esc(s.bold.join(' · '))}</strong></p>`;if(s.bullets.length)extra+=`<ul>${s.bullets.map(x=>`<li>${esc(x)}</li>`).join('')}</ul>`;else if(!s.subtitle&&!s.bold.length)extra+=`<p>Preview de ${esc(name||'deck.md')}</p>`;c.innerHTML=`<h3>${esc(s.title)}</h3>${extra}`;p.dataset.source=name||'deck.md'}
document.addEventListener('change',e=>{const input=e.target;if(!(input instanceof HTMLInputElement)||input.id!=='qf-md')return;const f=input.files&&input.files[0];if(!f)return;const r=new FileReader();r.onload=()=>updatePreview(String(r.result||''),f.name);r.readAsText(f)});
})();